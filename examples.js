console.info("page loaded");
      console.debug(2 + 3);
      console.warn(2 * (3 + 1));
      var employed = false;
      console.error(employed);
      var age = 26; // number

      var user = "Danny";
      user = user + "Faragau";

      var skills = { "html", "css"};

      var dan = {
          age: 26,
          employed: true
      };

      var motto = document.getElementById("job-title")